name: wireframe_theme_flutter
description: FLUX Wireframe CLI – Flutter UI Experience starter template with wireframe aesthetic
version: 1.0.0+1
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Wireframe theme from pub.dev
  wireframe_theme: ^1.0.4
  
  # State management and persistence
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  
  # Optional icon sets
  remixicon: ^1.0.0
  cupertino_icons: ^1.0.2
  
  # Permissions (if needed by your app)
  permission_handler: ^11.0.1

flutter:
  uses-material-design: true